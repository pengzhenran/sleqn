# 精确锁定的依赖版本（对应 conda 环境 sleqn）
#
# 前 4 行由 conda 提供，其余由 pip 安装；`packaging` 在 pip freeze 里会显示成
# 本地 file:// 路径（conda 打的包），这里改写成正常版本号，便于在别的机器上复现：
#
#   <env>\python.exe -m pip install -r requirements-lock.txt
#
# conda 提供的：python 3.12.14 / pip 26.2.1 / setuptools 84.0.0 / wheel 0.48.0 / packaging 26.3

# --- 运行时（随安装包分发）---
numpy==2.1.3
PySide6_Essentials==6.8.3
matplotlib==3.10.0
Cartopy==0.25.0

# --- 构建期 ---
pyinstaller==6.22.3

# --- 传递依赖 ---
shiboken6==6.8.3
contourpy==1.4.0
cycler==0.12.1
fonttools==4.65.0
kiwisolver==1.5.1
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
shapely==2.1.2
pyproj==3.8.0
pyshp==3.1.6
certifi==2026.7.22
altgraph==0.17.5
pefile==2024.8.26
pyinstaller-hooks-contrib==2026.7
pywin32-ctypes==0.2.3
